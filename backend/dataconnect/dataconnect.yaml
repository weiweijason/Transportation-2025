specVersion: "v1"
serviceId: "proj"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "proj-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
